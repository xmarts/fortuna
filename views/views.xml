<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="fortuna.list">
      <field name="name">fortuna list</field>
      <field name="model">fortuna.fortuna</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="fortuna.action_window">
      <field name="name">fortuna window</field>
      <field name="res_model">fortuna.fortuna</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="fortuna.action_server">
      <field name="name">fortuna server</field>
      <field name="model_id" ref="model_fortuna_fortuna"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="fortuna" id="fortuna.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="fortuna.menu_1" parent="fortuna.menu_root"/>
    <menuitem name="Menu 2" id="fortuna.menu_2" parent="fortuna.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="fortuna.menu_1_list" parent="fortuna.menu_1"
              action="fortuna.action_window"/>
    <menuitem name="Server to list" id="fortuna" parent="fortuna.menu_2"
              action="fortuna.action_server"/>
    -->
  </data>
</openerp>